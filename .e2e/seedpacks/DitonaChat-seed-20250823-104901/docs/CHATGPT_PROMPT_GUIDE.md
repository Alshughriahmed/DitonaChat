Read `docs/README_SEEDPACK.md` first. Focus on CSS vars (`--tb-h`, `--composer-h`, `--kb-pad`, `--safe-b`) and z-index layers.
Any layout change must re-measure heights (ResizeObserver / visualViewport). This pack is UI-only.
